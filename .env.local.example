# Local mode — no external services required.
# Override the SQLite file location if you want to put the DB elsewhere.
# Default is ./data/app.db (relative to the project root).
# SALARY_DB_PATH=D:\some\other\path\app.db
